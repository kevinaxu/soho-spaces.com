<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title1}} - {{title2}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .hero {
            position: relative;
            width: 100%;
            height: 50vh;
            overflow: hidden;
        }
        .hero video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .container {
            padding: 20px;
        }
        .before-after {
            display: flex;
            justify-content: space-between;
        }
        .before-after img {
            width: 48%;
        }
        .photo-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
        }
        .photo-grid img {
            width: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    {{> media}}

    {{> content}}

    {{#enableNavBarTransparentEffect}}
    <script>
        alert("enabled nav bar transparent")
    </script>
    {{/enableNavBarTransparentEffect}}

</body>
</html>